hi!

To run this yourself:

1. Keys
- populate them yourself using MongoDB Atlas and Firebase for Google authentication; see .example.env.local for an example .env.local

2. Run the application
- ``npm run build``
- ``pm2 start``
